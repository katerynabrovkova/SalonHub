// Placeholder — proves the post-login redirect for the "client" role lands
// here. The real client dashboard is a later stage.
export default function ClientPage() {
  return <p>Client dashboard placeholder</p>;
}
