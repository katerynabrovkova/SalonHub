// Placeholder — proves the post-login redirect for the "admin" role lands
// here. The real salon admin back-office is a later stage.
export default function AdminPage() {
  return <p>Admin dashboard placeholder</p>;
}
